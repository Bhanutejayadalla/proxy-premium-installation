import 'package:flutter/material.dart';

class AppColors {
  // Formal Mode (LinkedIn Style)
  static const formalBg = Color(0xFFF8FAFC);
  static const formalPrimary = Color(0xFF2563EB); // Blue
  static const formalText = Color(0xFF0F172A);
  static const formalAccent = Color(0xFF1E40AF);
  static const formalCard = Color(0xFFFFFFFF);

  // Casual Mode (Instagram Style)
  static const casualStart = Color(0xFFFFF7ED);
  static const casualEnd = Color(0xFFFDF2F8);
  static const casualPrimary = Color(0xFFEC4899); // Pink
  static const casualAccent = Color(0xFFDB2777);
  static const casualCard = Color(0xFFFFFFFF);

  // Professional Section
  static const proSkills = Color(0xFF6366F1);    // Indigo
  static const proExperience = Color(0xFF0891B2); // Cyan
  static const proEducation = Color(0xFF059669);   // Green
  static const proJobs = Color(0xFF7C3AED);        // Violet
  static const proBadge = Color(0xFF10B981);       // Emerald
  static const proDanger = Color(0xFFEF4444);      // Red

  // Status
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
}